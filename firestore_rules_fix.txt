rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Geçici kural: Giriş yapan kullanıcılar tüm verilere erişebilir
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 